<!-- <html>

    <head>
        <title> <%= title %> </title>
    </head>

    <body>

        partials
        <%- include('./_header'); -%> -->
        
        Profile<br>
        <%= locals.user.name %><br>
        <%= locals.user.email %>
        <%- include('./_post'); -%>
        
        <div>
            <h4>Friend</h4>
            <% for(u of users){ %>
                <small>
                    <a href="/users/profile/<%= u.id %>"><%=u.name %></a><br>
                    
                </small><br>
            <% } %>
        </div>
        <!-- partials -->
        <!-- <%- include('./_footer'); -%>
        
    </body>

</html> -->